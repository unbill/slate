## Fix auth hold

> Example Request

```shell
curl "<%= config.host %>/auth/566595c8ce88ccec69328566/fix-hold" \
  -H "<%= config.auth %>" \
  -X POST
```

> Example Response

```json
{
  "success": true
}
```

### HTTP Request

`POST <%= config.host %>/auth/{authId}/fix-hold`

### Arguments

Parameter | Description
----- | -----------
`authId` | ID of auth to fix.

### Returns

Returns an object with a `success` property if the hold was fixed. If the `authId` does not exist, this call returns an error.