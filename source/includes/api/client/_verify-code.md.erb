## Verify notification code

> Example Request

```shell
curl "<%= config.host %>/client/notification/verify-code" \
  -H "<%= config.auth %>" \
  -d '{
    "clientId": "56e1ddbd1a05319bc198d36f",
    "code": "512351"
  }'
```

> Example Response

```json
{
  "success": true
}
```

Verifies that a user has received the code sent to a `destination`.

### HTTP Request

`POST <%= config.host %>/client/notification/verify-code`

### Arguments

Parameter | Description
----- | -----------
`clientId` | ID of the client to verify the `code` with.
`code` | The code sent to the user's notification method.

### Returns

Returns an object with a `success` property if the notification method was successfully verified. Returns an error otherwise.
