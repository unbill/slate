# User API

To authenticate bills and retrieve balance and due date information, an Unbill `userId` is required. The API allows you to create an Unbill user and retrieve a `userId` for future requests.