## Search for companies

> Sample Request

```json
{
  "unbillKey": "<%= config.auth %>",
  "query": "Netflix"
}
```

> 200 Response

```json
[<%= config.companyResponseExample %>]
```

> 400 Response

```json
{
  "message": "Please provide search query."
}
```

Search for companies by name.

### HTTP Request

`POST <%= config.host %>/company/search`

### Post Parameters

Param | Description
----- | -----------
`query` | Search query.

### Returns

Returns a list of company objects that match the provided `query`.